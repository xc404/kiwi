# kiwi-bpmn-component-kafka

独立可选模块：`KafkaPublishActivity`（`@Component("kafkaPublish")`）。

## 接入

```xml
<dependency>
    <groupId>com.kiwi</groupId>
    <artifactId>kiwi-bpmn-component-kafka</artifactId>
</dependency>
```

## 说明

单次同步生产消息；消费场景请用 External Task 或独立入站服务。
