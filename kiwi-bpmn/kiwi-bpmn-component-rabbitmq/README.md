# kiwi-bpmn-component-rabbitmq

独立可选模块：`RabbitMqPublishActivity`（`@Component("rabbitMqPublish")`）。

## 接入

```xml
<dependency>
    <groupId>com.kiwi</groupId>
    <artifactId>kiwi-bpmn-component-rabbitmq</artifactId>
</dependency>
```
