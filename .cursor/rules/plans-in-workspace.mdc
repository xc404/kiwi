---
description: 计划文件 (plan) 必须保存在当前工作区内
alwaysApply: true
---

# 计划文件保存位置规则

## 规则
所有由 AI 助手生成的计划文件（plan 文件）**必须**保存在当前打开的工作区内，禁止保存到用户全局目录、系统临时目录或其他工作区之外的路径。

## 保存位置
- 默认路径：`<工作区根目录>/.cursor/plans/`
- 文件命名：`<功能描述>_<短哈希>.plan.md`（如 `frontend_intermediate_catch_event_82ba8a18.plan.md`）
- 如目录不存在，应自动创建

## 禁止的位置
- ❌ `C:\Users\<用户名>\` 或其他用户主目录
- ❌ `%TEMP%` 或系统临时目录
- ❌ 其他工作区目录（多工作区场景下，必须保存到该计划所属的主工作区）
- ❌ Cursor 全局配置目录（`~/.cursor/` 下而非 `<workspace>/.cursor/`）

## 多工作区场景
当前用户同时打开多个工作区时（如 `kiwi`、`cryo-em-server-backend`、`cryo-em-server-frontend`），应：
1. 判断计划主要涉及哪个工作区
2. 将 plan 保存到该工作区的 `.cursor/plans/` 下
3. 若计划跨多个工作区，保存到主要修改的工作区

## 理由
- 计划与代码同源管理，便于追溯
- 可随仓库共享/版本控制（如需要）
- 避免文件分散导致丢失
