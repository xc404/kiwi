# Tasks

- [x] Maven：引入 Spring AI BOM + OpenAI starter
- [x] 后端：`AiChatService` 改为 `ChatModel`；`application.yml` 使用 `spring.ai.openai`；精简 `kiwi.ai` 仅保留 enabled
- [x] 前端：改造 `ChatComponent` 对接 API + `embed` 样式；仪表盘 `ai-chat` 页嵌入 `app-chat`
