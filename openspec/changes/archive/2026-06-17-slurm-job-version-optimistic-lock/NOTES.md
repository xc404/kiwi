# 归档说明（未实施的空壳 change）

**日期：** 2026-06-17

本目录仅有 `.openspec.yaml`（`created: 2026-05-06`），**无** `proposal.md` / `design.md` / `tasks.md` / `specs/`，仓库内亦无对应的 `SlurmJob` 乐观锁版本字段实现。

视为已放弃或合并入其它 Slurm 变更（如 `slurm-complete-entrypoint-unification` 中提及的 optimistic lock 语义为既有行为保留，非本 change 范围）。

归档：`openspec archive --skip-specs --no-validate`
