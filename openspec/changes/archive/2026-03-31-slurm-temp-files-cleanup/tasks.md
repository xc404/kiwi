## 1. 配置与装配

- [x] 1.1 在 `SlurmProperties`（或 `kiwi.bpm.slurm` 下）增加 cleanup 相关字段：`enabled`、保留时长、调度表达式/间隔、可选文件名模式与递归开关
- [x] 1.2 在 `SlurmAutoConfiguration` 或等价处条件注册清理调度 Bean，并确保仅在 Slurm 工作目录配置有效时生效

## 2. 清理实现

- [x] 2.1 实现工作目录扫描与基于最后修改时间的过期判定，按设计限制删除范围（扩展名/后缀、默认非递归或受配置控制）
- [x] 2.2 使用 `@Scheduled` 触发清理；单文件失败记录日志且不阻断整批
- [x] 2.3 添加关键 INFO/WARN 日志（运行摘要、失败原因）

## 3. 验证与文档

- [x] 3.1 为清理逻辑补充单元测试（时间边界、关闭开关、单文件失败）
- [x] 3.2 在示例配置或 README 片段中说明默认保留策略与运维注意事项
